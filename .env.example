# Groq API Key (get it from https://console.groq.com/)
GROQ_API_KEY=enter yout api key here
GROQ_MODEL=openai/gpt-oss-20b

# Minimum cosine similarity score required to answer using document context (fallback to web search if below this)
SIMILARITY_THRESHOLD=0.15
